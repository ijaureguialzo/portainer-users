services:
  workspace:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - PYTHON_VERSION=${PYTHON_VERSION:-3.12}
    volumes:
      - ./.token:/root/.token:ro
      - ./scripts:/scripts:ro
